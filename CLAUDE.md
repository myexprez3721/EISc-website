# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a minimal static HTML project. Currently it contains a single page (`index.html`) that displays a centered "Hello World" heading.

## Development

No build system, package manager, or server is configured. Open `index.html` directly in a browser to preview it.
